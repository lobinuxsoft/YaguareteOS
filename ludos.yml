version: 1

name: YaguareteOS

# Anatase's own flatpaks/oci/cosign config lived here (S3-backed icon host,
# KMS-signed cosign chain) -- none of it is read by `ludos build`/`bootc
# create` (only by `ludos registry`/`upload` commands, which this project
# does not run: publishing goes through GHCR + our own classic Flatpak
# repo instead, see reference_flatpak_classic_repo_no_s3). Left out
# entirely rather than kept as dead config pointing at anatase-org's real
# infrastructure.
