From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 3 Aug 2026 02:18:58 +0200
Subject: add X11 passthrough permission

For certain microcompositors e.g., gamescope, and Steam to work together
within a Flatpak context, where one or both are in a Flatpak, they need
to be able to create propagate and propagate X11 sockets.

Specifically, under a normal Gamescope session, gamescope creates two
xWayland sockets, one for Steam and one for games. If Steam is running
within a Flatpak, currently it can only access the primary socket.
This is problematic as games will launch within it, and not have access
to the game xwayland session.

Therefore, add --socket=x11-passthrough, which propagates all x11
sockets from the host to the client.
---
 common/flatpak-context-private.h |  1 +
 common/flatpak-context.c         |  1 +
 common/flatpak-run-sockets.c     |  6 ++++--
 common/flatpak-run-x11-private.h |  3 ++-
 common/flatpak-run-x11.c         | 22 ++++++++++++++++++----
 portal/flatpak-portal.c          |  5 ++++-
 6 files changed, 30 insertions(+), 8 deletions(-)

diff --git a/common/flatpak-context-private.h b/common/flatpak-context-private.h
index 56ce95734a26..0bf7874d4f3e 100644
--- a/common/flatpak-context-private.h
+++ b/common/flatpak-context-private.h
@@ -61,6 +61,7 @@ typedef enum {
   FLATPAK_CONTEXT_SOCKET_CUPS        = 1 << 8,
   FLATPAK_CONTEXT_SOCKET_GPG_AGENT   = 1 << 9,
   FLATPAK_CONTEXT_SOCKET_INHERIT_WAYLAND_SOCKET = 1 << 10,
+  FLATPAK_CONTEXT_SOCKET_X11_UNRESTRICTED       = 1 << 11,
 } FlatpakContextSockets;
 
 typedef enum {
diff --git a/common/flatpak-context.c b/common/flatpak-context.c
index 0a35a41ca4cf..af98128f5de3 100644
--- a/common/flatpak-context.c
+++ b/common/flatpak-context.c
@@ -67,6 +67,7 @@ const char *flatpak_context_sockets[] = {
   "cups",
   "gpg-agent",
   "inherit-wayland-socket",
+  "x11-passthrough",
   NULL
 };
 
diff --git a/common/flatpak-run-sockets.c b/common/flatpak-run-sockets.c
index f9d0547c468d..2d8e91e16a76 100644
--- a/common/flatpak-run-sockets.c
+++ b/common/flatpak-run-sockets.c
@@ -179,6 +179,7 @@ flatpak_run_add_socket_args_environment (FlatpakBwrap         *bwrap,
   gboolean allow_wayland;
   gboolean inherit_wayland_socket;
   gboolean allow_x11;
+  gboolean unrestricted_x11;
 
   allow_wayland = (sockets & FLATPAK_CONTEXT_SOCKET_WAYLAND) != 0;
   inherit_wayland_socket = (sockets & FLATPAK_CONTEXT_SOCKET_INHERIT_WAYLAND_SOCKET) != 0;
@@ -187,8 +188,9 @@ flatpak_run_add_socket_args_environment (FlatpakBwrap         *bwrap,
                                 allow_wayland,
                                 inherit_wayland_socket);
 
-  allow_x11 = (sockets & FLATPAK_CONTEXT_SOCKET_X11) != 0;
-  flatpak_run_add_x11_args (bwrap, allow_x11, shares);
+  unrestricted_x11 = (sockets & FLATPAK_CONTEXT_SOCKET_X11_UNRESTRICTED) != 0;
+  allow_x11 = unrestricted_x11 || (sockets & FLATPAK_CONTEXT_SOCKET_X11) != 0;
+  flatpak_run_add_x11_args (bwrap, allow_x11, shares, unrestricted_x11);
 
   if (sockets & FLATPAK_CONTEXT_SOCKET_SSH_AUTH)
     {
diff --git a/common/flatpak-run-x11-private.h b/common/flatpak-run-x11-private.h
index 96da2e6ba4ea..d3741447de54 100644
--- a/common/flatpak-run-x11-private.h
+++ b/common/flatpak-run-x11-private.h
@@ -30,7 +30,8 @@ G_BEGIN_DECLS
 
 void flatpak_run_add_x11_args (FlatpakBwrap         *bwrap,
                                gboolean              allowed,
-                               FlatpakContextShares  shares);
+                               FlatpakContextShares  shares,
+                               gboolean              unrestricted);
 
 gboolean flatpak_run_parse_x11_display (const char  *display,
                                         int         *family,
diff --git a/common/flatpak-run-x11.c b/common/flatpak-run-x11.c
index 90931c2ea267..b6f2ca1d53cf 100644
--- a/common/flatpak-run-x11.c
+++ b/common/flatpak-run-x11.c
@@ -202,15 +202,20 @@ flatpak_run_parse_x11_display (const char  *display,
 void
 flatpak_run_add_x11_args (FlatpakBwrap         *bwrap,
                           gboolean              allowed,
-                          FlatpakContextShares  shares)
+                          FlatpakContextShares  shares,
+                          gboolean              unrestricted)
 {
   g_autofree char *x11_socket = NULL;
   const char *display;
   g_autoptr(GError) local_error = NULL;
 
-  /* Always cover /tmp/.X11-unix, that way we never see the host one in case
-   * we have access to the host /tmp. If you request X access we'll put the right
-   * thing in this anyway.
+  /* Cover /tmp/.X11-unix, that way we never see the host one in case we have
+   * access to the host /tmp. If you request X access we'll put the right thing
+   * in this anyway.
+   * 
+   * For specialized instances, such as Flatpak microcompositors, allow binding
+   * the host directory so that the application can consume all X11 sockets and
+   * create new ones as needed.
    *
    * We need to be a bit careful here, because there are two situations in
    * which potentially hostile processes have access to /tmp and could
@@ -229,6 +234,15 @@ flatpak_run_add_x11_args (FlatpakBwrap         *bwrap,
    *   /tmp/.X11-unix is already a mount point, meaning the app cannot
    *   rename or delete it.
    */
+  if (unrestricted) {
+    flatpak_bwrap_add_args(bwrap,
+                           "--bind", "/tmp/.X11-unix", "/tmp/.X11-unix",
+                           NULL);
+    // TODO: Handle xauth and perhaps using the default display attr
+    // from the host if not provided
+    return;
+  }
+
   flatpak_bwrap_add_args (bwrap,
                           "--tmpfs", "/tmp/.X11-unix",
                           NULL);
diff --git a/portal/flatpak-portal.c b/portal/flatpak-portal.c
index 3bd68e2847c2..fba770927d3d 100644
--- a/portal/flatpak-portal.c
+++ b/portal/flatpak-portal.c
@@ -968,13 +968,16 @@ handle_spawn (PortalFlatpak         *object,
         {
           if (sockets != NULL && g_strv_contains ((const char * const *) sockets, "wayland"))
             g_ptr_array_add (flatpak_argv, g_strdup ("--socket=wayland"));
+          if (sockets != NULL && g_strv_contains ((const char * const *) sockets, "x11-passthrough"))
+            g_ptr_array_add (flatpak_argv, g_strdup ("--socket=x11-passthrough"));
           if (sockets != NULL && g_strv_contains ((const char * const *) sockets, "fallback-x11"))
             g_ptr_array_add (flatpak_argv, g_strdup ("--socket=fallback-x11"));
           if (sockets != NULL && g_strv_contains ((const char * const *) sockets, "x11"))
             g_ptr_array_add (flatpak_argv, g_strdup ("--socket=x11"));
           if (shares != NULL && g_strv_contains ((const char * const *) shares, "ipc") &&
               sockets != NULL && (g_strv_contains ((const char * const *) sockets, "fallback-x11") ||
-                                  g_strv_contains ((const char * const *) sockets, "x11")))
+                                  g_strv_contains ((const char * const *) sockets, "x11") ||
+                                  g_strv_contains ((const char * const *) sockets, "x11-passthrough")))
             g_ptr_array_add (flatpak_argv, g_strdup ("--share=ipc"));
         }
       if (sandbox_flags & FLATPAK_SPAWN_SANDBOX_FLAGS_SHARE_SOUND)

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Thu, 3 Sep 2026 21:56:11 +0200
Subject: Export KDE file manager service menus

---
 app/flatpak-builtins-build-finish.c |  1 +
 common/flatpak-context.c            |  4 ++++
 common/flatpak-dir.c                |  1 +
 tests/make-test-app.sh              | 23 +++++++++++++++++++++++
 tests/test-run.sh                   |  3 +++
 5 files changed, 32 insertions(+)

diff --git a/app/flatpak-builtins-build-finish.c b/app/flatpak-builtins-build-finish.c
index 96a794574e4f..155ba94efd46 100644
--- a/app/flatpak-builtins-build-finish.c
+++ b/app/flatpak-builtins-build-finish.c
@@ -237,6 +237,7 @@ collect_exports (GFile          *base,
     "share/dbus-1/services",              /* D-Bus service files */
     "share/gnome-shell/search-providers", /* Search providers */
     "share/krunner/dbusplugins",          /* KDE krunner DBus plugins */
+    "share/kio/servicemenus",             /* KDE file manager service menus */
     "share/appdata",                      /* Copy appdata/metainfo files (legacy path) */
     "share/metainfo",                     /* Copy appdata/metainfo files */
     NULL,
diff --git a/common/flatpak-context.c b/common/flatpak-context.c
index af98128f5de3..bbc9ac786bd4 100644
--- a/common/flatpak-context.c
+++ b/common/flatpak-context.c
@@ -4360,6 +4360,10 @@ flatpak_context_get_allowed_exports (FlatpakContext *context,
     {
       g_ptr_array_add (allowed_extensions, g_strdup (".desktop"));
     }
+  else if (strcmp (source_path, "share/kio/servicemenus") == 0)
+    {
+      g_ptr_array_add (allowed_extensions, g_strdup (".desktop"));
+    }
   else if (strcmp (source_path, "share/mime/packages") == 0)
     {
       g_ptr_array_add (allowed_extensions, g_strdup (".xml"));
diff --git a/common/flatpak-dir.c b/common/flatpak-dir.c
index c05961c6d421..680becfa37cf 100644
--- a/common/flatpak-dir.c
+++ b/common/flatpak-dir.c
@@ -9058,6 +9058,7 @@ flatpak_export_dir (GFile        *source,
     "share/dbus-1/services",               "../../..",
     "share/gnome-shell/search-providers",  "../../..",
     "share/krunner/dbusplugins",           "../../..",
+    "share/kio/servicemenus",              "../../..",
     "share/mime/packages",                 "../../..",
     "share/metainfo",                      "../..",
     "bin",                                 "..",
diff --git a/tests/make-test-app.sh b/tests/make-test-app.sh
index 21f7d5bd5e48..adec709608d7 100755
--- a/tests/make-test-app.sh
+++ b/tests/make-test-app.sh
@@ -128,6 +128,29 @@ X-Plasma-DBusRunner-Service=org.test.Hello.KRunner
 X-Plasma-DBusRunner-Path=/org/test/Hello/KRunner
 EOF
 
+mkdir -p ${DIR}/files/share/kio/servicemenus
+cat > ${DIR}/files/share/kio/servicemenus/org.test.Hello.desktop <<EOF
+[Desktop Entry]
+Type=Service
+MimeType=text/plain;
+Actions=hello;
+X-KDE-Priority=TopLevel
+
+[Desktop Action hello]
+Name=Say hello
+Exec=hello.sh --service-menu %F
+EOF
+cat > ${DIR}/files/share/kio/servicemenus/dont-export.desktop <<EOF
+[Desktop Entry]
+Type=Service
+MimeType=text/plain;
+Actions=hello;
+
+[Desktop Action hello]
+Name=Do not export
+Exec=hello.sh %F
+EOF
+
 mkdir -p ${DIR}/files/share/icons/hicolor/64x64/apps
 cp $(dirname $0)/org.test.Hello.png ${DIR}/files/share/icons/hicolor/64x64/apps/${APP_ID}.png
 cp $(dirname $0)/org.test.Hello.png ${DIR}/files/share/icons/hicolor/64x64/apps/dont-export.png
diff --git a/tests/test-run.sh b/tests/test-run.sh
index cbe0cda3be4b..ca5d557b08c2 100644
--- a/tests/test-run.sh
+++ b/tests/test-run.sh
@@ -50,6 +50,9 @@ assert_has_file $FL_DIR/exports/share/gnome-shell/search-providers/org.test.Hell
 assert_file_has_content $FL_DIR/exports/share/gnome-shell/search-providers/org.test.Hello.search-provider.ini "^DefaultDisabled=true$"
 assert_has_file $FL_DIR/exports/share/krunner/dbusplugins/org.test.Hello.desktop
 assert_file_has_content $FL_DIR/exports/share/krunner/dbusplugins/org.test.Hello.desktop "^X-KDE-PluginInfo-EnabledByDefault=false$"
+assert_has_file $FL_DIR/exports/share/kio/servicemenus/org.test.Hello.desktop
+assert_file_has_content $FL_DIR/exports/share/kio/servicemenus/org.test.Hello.desktop "^Exec=.*flatpak run --branch=stable --arch=$ARCH --command=hello\.sh --file-forwarding org\.test\.Hello --service-menu @@ %F @@$"
+assert_not_has_file $FL_DIR/exports/share/kio/servicemenus/dont-export.desktop
 assert_has_file $FL_DIR/exports/share/icons/hicolor/64x64/apps/org.test.Hello.png
 assert_not_has_file $FL_DIR/exports/share/icons/hicolor/64x64/apps/dont-export.png
 assert_has_file $FL_DIR/exports/share/icons/HighContrast/64x64/apps/org.test.Hello.png

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Thu, 3 Sep 2026 22:00:42 +0200
Subject: Export Plasma application launcher actions

---
 app/flatpak-builtins-build-finish.c |  1 +
 common/flatpak-context.c            |  4 ++++
 common/flatpak-dir.c                |  1 +
 tests/make-test-app.sh              | 11 +++++++++++
 tests/test-run.sh                   |  2 ++
 5 files changed, 19 insertions(+)

diff --git a/app/flatpak-builtins-build-finish.c b/app/flatpak-builtins-build-finish.c
index 155ba94efd46..ae9cbf653ba4 100644
--- a/app/flatpak-builtins-build-finish.c
+++ b/app/flatpak-builtins-build-finish.c
@@ -238,6 +238,7 @@ collect_exports (GFile          *base,
     "share/gnome-shell/search-providers", /* Search providers */
     "share/krunner/dbusplugins",          /* KDE krunner DBus plugins */
     "share/kio/servicemenus",             /* KDE file manager service menus */
+    "share/plasma/kickeractions",          /* Plasma application launcher actions */
     "share/appdata",                      /* Copy appdata/metainfo files (legacy path) */
     "share/metainfo",                     /* Copy appdata/metainfo files */
     NULL,
diff --git a/common/flatpak-context.c b/common/flatpak-context.c
index bbc9ac786bd4..c8a2fa65317a 100644
--- a/common/flatpak-context.c
+++ b/common/flatpak-context.c
@@ -4364,6 +4364,10 @@ flatpak_context_get_allowed_exports (FlatpakContext *context,
     {
       g_ptr_array_add (allowed_extensions, g_strdup (".desktop"));
     }
+  else if (strcmp (source_path, "share/plasma/kickeractions") == 0)
+    {
+      g_ptr_array_add (allowed_extensions, g_strdup (".desktop"));
+    }
   else if (strcmp (source_path, "share/mime/packages") == 0)
     {
       g_ptr_array_add (allowed_extensions, g_strdup (".xml"));
diff --git a/common/flatpak-dir.c b/common/flatpak-dir.c
index 680becfa37cf..cfd47b649526 100644
--- a/common/flatpak-dir.c
+++ b/common/flatpak-dir.c
@@ -9059,6 +9059,7 @@ flatpak_export_dir (GFile        *source,
     "share/gnome-shell/search-providers",  "../../..",
     "share/krunner/dbusplugins",           "../../..",
     "share/kio/servicemenus",              "../../..",
+    "share/plasma/kickeractions",           "../../..",
     "share/mime/packages",                 "../../..",
     "share/metainfo",                      "../..",
     "bin",                                 "..",
diff --git a/tests/make-test-app.sh b/tests/make-test-app.sh
index adec709608d7..20c84b4b29e9 100755
--- a/tests/make-test-app.sh
+++ b/tests/make-test-app.sh
@@ -151,6 +151,17 @@ Name=Do not export
 Exec=hello.sh %F
 EOF
 
+mkdir -p ${DIR}/files/share/plasma/kickeractions
+cat > ${DIR}/files/share/plasma/kickeractions/org.test.Hello.desktop <<EOF
+[Desktop Entry]
+Type=Service
+Actions=hello;
+
+[Desktop Action hello]
+Name=Say hello
+Exec=hello.sh --launcher-action %u
+EOF
+
 mkdir -p ${DIR}/files/share/icons/hicolor/64x64/apps
 cp $(dirname $0)/org.test.Hello.png ${DIR}/files/share/icons/hicolor/64x64/apps/${APP_ID}.png
 cp $(dirname $0)/org.test.Hello.png ${DIR}/files/share/icons/hicolor/64x64/apps/dont-export.png
diff --git a/tests/test-run.sh b/tests/test-run.sh
index ca5d557b08c2..1b1e44148a69 100644
--- a/tests/test-run.sh
+++ b/tests/test-run.sh
@@ -53,6 +53,8 @@ assert_file_has_content $FL_DIR/exports/share/krunner/dbusplugins/org.test.Hello
 assert_has_file $FL_DIR/exports/share/kio/servicemenus/org.test.Hello.desktop
 assert_file_has_content $FL_DIR/exports/share/kio/servicemenus/org.test.Hello.desktop "^Exec=.*flatpak run --branch=stable --arch=$ARCH --command=hello\.sh --file-forwarding org\.test\.Hello --service-menu @@ %F @@$"
 assert_not_has_file $FL_DIR/exports/share/kio/servicemenus/dont-export.desktop
+assert_has_file $FL_DIR/exports/share/plasma/kickeractions/org.test.Hello.desktop
+assert_file_has_content $FL_DIR/exports/share/plasma/kickeractions/org.test.Hello.desktop "^Exec=.*flatpak run --branch=stable --arch=$ARCH --command=hello\.sh --file-forwarding org\.test\.Hello --launcher-action @@u %u @@$"
 assert_has_file $FL_DIR/exports/share/icons/hicolor/64x64/apps/org.test.Hello.png
 assert_not_has_file $FL_DIR/exports/share/icons/hicolor/64x64/apps/dont-export.png
 assert_has_file $FL_DIR/exports/share/icons/HighContrast/64x64/apps/org.test.Hello.png

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Sat, 12 Sep 2026 19:17:31 +0200
Subject: Invalidate linker cache for mutable runtimes

---
 common/flatpak-run.c | 30 ++++++++++++++++++++++++++++++
 tests/test-run.sh    | 17 +++++++++++++++++
 2 files changed, 47 insertions(+)

diff --git a/common/flatpak-run.c b/common/flatpak-run.c
index 4c05f266a3bb..e091e035dc7f 100644
--- a/common/flatpak-run.c
+++ b/common/flatpak-run.c
@@ -2842,13 +2842,42 @@ flatpak_context_load_for_deploy (FlatpakDeploy *deploy,
 static char *
 calculate_ld_cache_checksum (GBytes   *app_deploy_data,
                              GBytes   *runtime_deploy_data,
+                             int       runtime_fd,
                              const char *app_extensions,
                              const char *runtime_extensions)
 {
   g_autoptr(GChecksum) ld_so_checksum = g_checksum_new (G_CHECKSUM_SHA256);
+  glnx_autofd int runtime_ld_so_cache_path_fd = -1;
+  glnx_autofd int runtime_ld_so_cache_fd = -1;
+  g_autoptr(GBytes) runtime_ld_so_cache = NULL;
+
   if (app_deploy_data)
     g_checksum_update (ld_so_checksum, (guchar *) flatpak_deploy_data_get_commit (app_deploy_data), -1);
   g_checksum_update (ld_so_checksum, (guchar *) flatpak_deploy_data_get_commit (runtime_deploy_data), -1);
+
+  /* Normally the runtime commit is sufficient to identify its contents. Some
+   * system integrations, however, expose a mutable runtime tree behind a
+   * stable deploy ID. Include the runtime's input linker cache so that changes
+   * to the libraries it describes invalidate our generated cache too. */
+  runtime_ld_so_cache_path_fd = glnx_chaseat (runtime_fd, "etc/ld.so.cache",
+                                              GLNX_CHASE_RESOLVE_BENEATH |
+                                              GLNX_CHASE_MUST_BE_REGULAR,
+                                              NULL);
+  if (runtime_ld_so_cache_path_fd >= 0)
+    runtime_ld_so_cache_fd = glnx_fd_reopen (runtime_ld_so_cache_path_fd,
+                                             O_RDONLY, NULL);
+  if (runtime_ld_so_cache_fd >= 0)
+    runtime_ld_so_cache = glnx_fd_readall_bytes (runtime_ld_so_cache_fd, NULL, NULL);
+
+  if (runtime_ld_so_cache != NULL)
+    {
+      gconstpointer data;
+      gsize size;
+
+      data = g_bytes_get_data (runtime_ld_so_cache, &size);
+      g_checksum_update (ld_so_checksum, data, size);
+    }
+
   if (app_extensions)
     g_checksum_update (ld_so_checksum, (guchar *) app_extensions, -1);
   if (runtime_extensions)
@@ -3767,6 +3796,7 @@ flatpak_run_app (FlatpakDecomposed   *app_ref,
   if (use_ld_so_cache)
     {
       checksum = calculate_ld_cache_checksum (app_deploy_data, runtime_deploy_data,
+                                              runtime_fd,
                                               app_extensions, runtime_extensions);
       ld_so_fd = regenerate_ld_cache (bwrap->argv,
                                       bwrap->fds,
diff --git a/tests/test-run.sh b/tests/test-run.sh
index 1b1e44148a69..d851fbdff388 100644
--- a/tests/test-run.sh
+++ b/tests/test-run.sh
@@ -89,6 +89,23 @@ assert_file_has_content hello_out '^Hello world, from a sandbox$'
 
 ok "hello"
 
+# A runtime is normally immutable and identified by its commit, but some
+# system integrations expose mutable runtime contents behind a stable deploy
+# ID. Ensure an updated runtime linker cache invalidates Flatpak's generated
+# linker cache in that case.
+runtime_ld_cache="$FL_DIR/runtime/org.test.Platform/$ARCH/stable/active/files/etc/ld.so.cache"
+app_ld_cache_dir="$TEST_DATA_DIR/home/.var/app/org.test.Hello/.ld.so"
+mkdir -p "$(dirname "$runtime_ld_cache")"
+printf 'runtime linker cache version 1\n' > "$runtime_ld_cache"
+run org.test.Hello > /dev/null
+first_ld_cache=$(readlink "$app_ld_cache_dir/active")
+printf 'runtime linker cache version 2\n' > "$runtime_ld_cache"
+run org.test.Hello > /dev/null
+second_ld_cache=$(readlink "$app_ld_cache_dir/active")
+assert_not_streq "$first_ld_cache" "$second_ld_cache"
+
+ok "runtime linker cache invalidates generated linker cache"
+
 # This should try and fail to run e.g. /usr/bin/--tmpfs, which will
 # exit with a nonzero status because there is no such executable.
 # It should not pass "--tmpfs /blah hello.sh" as bwrap options.
