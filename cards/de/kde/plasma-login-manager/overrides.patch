From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 13 Jul 2026 20:03:45 +0200
Subject: drop soft reboot breaking autologin

yeah, bypass, then again once your drive is unlocked it is unlocked and it makes the feature useless

Interim solution even so
---
 src/daemon/DaemonApp.cpp | 22 +---------------------
 1 file changed, 1 insertion(+), 21 deletions(-)

diff --git a/src/daemon/DaemonApp.cpp b/src/daemon/DaemonApp.cpp
index f2171ad30601..d8a99efbb611 100644
--- a/src/daemon/DaemonApp.cpp
+++ b/src/daemon/DaemonApp.cpp
@@ -92,27 +92,7 @@ bool DaemonApp::tryLockFirstLogin()
     }
     m_firstloginLock = true;
 
-    QDBusMessage msg = QDBusMessage::createMethodCall(QStringLiteral("org.freedesktop.systemd1"),
-                                                      QStringLiteral("/org/freedesktop/systemd1"),
-                                                      QStringLiteral("org.freedesktop.DBus.Properties"),
-                                                      QStringLiteral("Get"));
-
-    msg << QStringLiteral("org.freedesktop.systemd1.Manager") << QStringLiteral("SoftRebootsCount");
-
-    const QDBusMessage reply = QDBusConnection::systemBus().call(msg);
-
-    if (reply.type() == QDBusMessage::ErrorMessage) {
-        qWarning() << "DBus error:" << reply.errorName() << "-" << reply.errorMessage();
-        return true;
-    }
-
-    const QVariant soft_reboot_count = qvariant_cast<QDBusVariant>(reply.arguments().at(0)).variant();
-    if (!soft_reboot_count.isValid()) {
-        qWarning() << "DBus variant is invalid:" << reply;
-        return true;
-    }
-
-    return soft_reboot_count.toUInt() == 0;
+    return true;
 };
 }
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Sun, 9 Aug 2026 02:53:47 +0200
Subject: use our theme, not breeze light

---
 src/frontend/startkde/lookandfeelsettings.kcfg | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/src/frontend/startkde/lookandfeelsettings.kcfg b/src/frontend/startkde/lookandfeelsettings.kcfg
index ad9d359fdb1b..445fddcfeb7a 100644
--- a/src/frontend/startkde/lookandfeelsettings.kcfg
+++ b/src/frontend/startkde/lookandfeelsettings.kcfg
@@ -7,15 +7,15 @@
   <group name="KDE">
     <entry name="lookAndFeelPackage" key="LookAndFeelPackage" type="String">
       <label>Global look and feel</label>
-      <default>org.kde.breeze.desktop</default>
+      <default>org.yaguarete.desktop</default>
     </entry>
     <entry name="defaultLightLookAndFeel" key="DefaultLightLookAndFeel" type="String">
       <label>Global Look and Feel package, alternate</label>
-      <default>org.kde.breeze.desktop</default>
+      <default>org.yaguarete.desktop</default>
     </entry>
     <entry name="defaultDarkLookAndFeel" key="DefaultDarkLookAndFeel" type="String">
       <label>Global Look and Feel package, alternate</label>
-      <default>org.kde.breezedark.desktop</default>
+      <default>org.yaguarete.desktop</default>
     </entry>
     <entry name="automaticLookAndFeel" key="AutomaticLookAndFeel" type="Bool">
       <label>Switch between dark and light global themes depending on time of day</label>

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Sun, 9 Aug 2026 02:54:01 +0200
Subject: catch sighup so plasmalogin does not crash

---
 src/helper/HelperApp.cpp | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/src/helper/HelperApp.cpp b/src/helper/HelperApp.cpp
index 61553d39e07c..acaacb9ac37d 100644
--- a/src/helper/HelperApp.cpp
+++ b/src/helper/HelperApp.cpp
@@ -44,9 +44,10 @@ HelperApp::HelperApp(int &argc, char **argv)
 {
     qInstallMessageHandler(HelperMessageHandler);
     auto sig = KSignalHandler::self();
+    sig->watchSignal(SIGHUP);
     sig->watchSignal(SIGTERM);
     QObject::connect(sig, &KSignalHandler::signalReceived, m_session, [](int s) {
-        if (s == SIGTERM) {
+        if (s == SIGHUP || s == SIGTERM) {
             QCoreApplication::instance()->exit(-1);
         }
     });

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Thu, 20 Aug 2026 00:18:31 +0200
Subject: make controller presses translate to enter to attempt a login

---
 CMakeLists.txt                           |  2 +
 src/frontend/greeter/CMakeLists.txt      |  2 +
 src/frontend/greeter/controllerinput.cpp | 92 ++++++++++++++++++++++++
 src/frontend/greeter/controllerinput.h   | 25 +++++++
 src/frontend/greeter/main.cpp            | 17 +++++
 5 files changed, 138 insertions(+)
 create mode 100644 src/frontend/greeter/controllerinput.cpp
 create mode 100644 src/frontend/greeter/controllerinput.h

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 52af3b61b437..b03e19e22845 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -58,6 +58,8 @@ pkg_check_modules(SYSTEMD REQUIRED "systemd")
 
 pkg_check_modules(LIBSYSTEMD REQUIRED "libsystemd" IMPORTED_TARGET)
 
+pkg_check_modules(LIBUDEV REQUIRED "libudev" IMPORTED_TARGET)
+
 if (NOT DEFINED SYSTEMD_SYSTEM_UNIT_DIR)
     pkg_get_variable(SYSTEMD_SYSTEM_UNIT_DIR systemd systemdsystemunitdir)
 endif()
diff --git a/src/frontend/greeter/CMakeLists.txt b/src/frontend/greeter/CMakeLists.txt
index be8ea32e05d1..ce3c2602f590 100644
--- a/src/frontend/greeter/CMakeLists.txt
+++ b/src/frontend/greeter/CMakeLists.txt
@@ -10,6 +10,7 @@ set(LOGIN_GREETER_SRCS
     backend/SocketWriter.cpp
     mockbackend/MockGreeterProxy.cpp
     blurscreenbridge.cpp
+    controllerinput.cpp
     greetereventfilter.cpp
 )
 
@@ -56,6 +57,7 @@ target_link_libraries(plasma-login-greeter PRIVATE
     LayerShellQt::Interface
     PW::KWorkspace
     KF6::ScreenDpms
+    PkgConfig::LIBUDEV
 )
 
 ecm_finalize_qml_module(plasma-login-greeter)
diff --git a/src/frontend/greeter/controllerinput.cpp b/src/frontend/greeter/controllerinput.cpp
new file mode 100644
index 000000000000..fbe369067138
--- /dev/null
+++ b/src/frontend/greeter/controllerinput.cpp
@@ -0,0 +1,92 @@
+/*
+ * SPDX-FileCopyrightText: 2026 KDE Contributors
+ *
+ * SPDX-License-Identifier: LGPL-2.0-or-later
+ */
+
+#include "controllerinput.h"
+
+#include <QDebug>
+#include <QSocketNotifier>
+
+#include <cerrno>
+#include <fcntl.h>
+#include <linux/input-event-codes.h>
+#include <linux/input.h>
+#include <unistd.h>
+
+#include <libudev.h>
+
+namespace
+{
+bool isControllerButton(unsigned int code)
+{
+    return (code >= BTN_MISC && code < BTN_MOUSE) || (code >= BTN_JOYSTICK && code < BTN_DIGI) || (code >= BTN_GEAR_DOWN && code <= BTN_GEAR_UP)
+        || (code >= BTN_DPAD_UP && code <= BTN_GRIPR2) || (code >= BTN_TRIGGER_HAPPY1 && code <= BTN_TRIGGER_HAPPY40);
+}
+}
+
+ControllerInput::ControllerInput(QObject *parent)
+    : QObject(parent)
+{
+    udev *context = udev_new();
+    if (!context) {
+        return;
+    }
+
+    udev_enumerate *enumerate = udev_enumerate_new(context);
+    if (enumerate) {
+        udev_enumerate_add_match_subsystem(enumerate, "input");
+        udev_enumerate_add_match_property(enumerate, "ID_INPUT_JOYSTICK", "1");
+        udev_enumerate_scan_devices(enumerate);
+
+        udev_list_entry *entry = nullptr;
+        udev_list_entry_foreach(entry, udev_enumerate_get_list_entry(enumerate))
+        {
+            udev_device *device = udev_device_new_from_syspath(context, udev_list_entry_get_name(entry));
+            const char *devnode = device ? udev_device_get_devnode(device) : nullptr;
+            const char *sysname = device ? udev_device_get_sysname(device) : nullptr;
+            if (devnode && sysname && qstrncmp(sysname, "event", 5) == 0) {
+                const int fd = open(devnode, O_RDONLY | O_NONBLOCK | O_CLOEXEC);
+                if (fd >= 0) {
+                    m_deviceFds.append(fd);
+                    auto *notifier = new QSocketNotifier(fd, QSocketNotifier::Read, this);
+                    connect(notifier, &QSocketNotifier::activated, this, [this, fd, notifier] {
+                        input_event events[16];
+                        const ssize_t bytesRead = read(fd, events, sizeof(events));
+                        if (bytesRead < 0 && errno == EAGAIN) {
+                            return;
+                        }
+                        if (bytesRead <= 0) {
+                            notifier->setEnabled(false);
+                            m_deviceFds.removeOne(fd);
+                            close(fd);
+                            return;
+                        }
+
+                        const size_t eventCount = bytesRead / sizeof(input_event);
+                        for (size_t i = 0; i < eventCount; ++i) {
+                            if (events[i].type == EV_KEY && events[i].value == 1 && isControllerButton(events[i].code)) {
+                                Q_EMIT buttonPressed();
+                            }
+                        }
+                    });
+                } else {
+                    qWarning() << "Unable to open controller input device" << devnode << ':' << qt_error_string(errno);
+                }
+            }
+            if (device) {
+                udev_device_unref(device);
+            }
+        }
+        udev_enumerate_unref(enumerate);
+    }
+    udev_unref(context);
+}
+
+ControllerInput::~ControllerInput()
+{
+    for (int fd : m_deviceFds) {
+        close(fd);
+    }
+}
diff --git a/src/frontend/greeter/controllerinput.h b/src/frontend/greeter/controllerinput.h
new file mode 100644
index 000000000000..fda3933dc244
--- /dev/null
+++ b/src/frontend/greeter/controllerinput.h
@@ -0,0 +1,25 @@
+/*
+ * SPDX-FileCopyrightText: 2026 KDE Contributors
+ *
+ * SPDX-License-Identifier: LGPL-2.0-or-later
+ */
+
+#pragma once
+
+#include <QList>
+#include <QObject>
+
+class ControllerInput : public QObject
+{
+    Q_OBJECT
+
+public:
+    explicit ControllerInput(QObject *parent = nullptr);
+    ~ControllerInput() override;
+
+Q_SIGNALS:
+    void buttonPressed();
+
+private:
+    QList<int> m_deviceFds;
+};
diff --git a/src/frontend/greeter/main.cpp b/src/frontend/greeter/main.cpp
index 699e2ad0c39a..ecd71d3f5ccc 100644
--- a/src/frontend/greeter/main.cpp
+++ b/src/frontend/greeter/main.cpp
@@ -7,9 +7,11 @@
 #include <QCommandLineOption>
 #include <QCommandLineParser>
 #include <QGuiApplication>
+#include <QKeyEvent>
 #include <QObject>
 #include <QQmlContext>
 #include <QScreen>
+#include <QWindow>
 
 #include <KLocalizedString>
 #include <KWindowSystem>
@@ -22,6 +24,7 @@
 #include "mockbackend/MockGreeterProxy.h"
 
 #include "blurscreenbridge.h"
+#include "controllerinput.h"
 #include "greetereventfilter.h"
 #include "models/sessionmodel.h"
 #include "models/usermodel.h"
@@ -35,7 +38,20 @@ public:
     explicit LoginGreeter(QObject *parent = nullptr)
         : QObject(parent)
         , m_engine(new PlasmaQuick::SharedQmlEngine)
+        , m_controllerInput(new ControllerInput(this))
     {
+        connect(m_controllerInput, &ControllerInput::buttonPressed, this, [] {
+            QWindow *window = qGuiApp->focusWindow();
+            if (!window) {
+                return;
+            }
+
+            QKeyEvent pressEvent(QEvent::KeyPress, Qt::Key_Return, Qt::NoModifier, QStringLiteral("\r"));
+            QCoreApplication::sendEvent(window, &pressEvent);
+            QKeyEvent releaseEvent(QEvent::KeyRelease, Qt::Key_Return, Qt::NoModifier, QStringLiteral("\r"));
+            QCoreApplication::sendEvent(window, &releaseEvent);
+        });
+
         connect(qApp, &QGuiApplication::screenAdded, this, [this](QScreen *screen) {
             createWindowForScreen(screen);
         });
@@ -92,6 +108,7 @@ private:
     }
 
     PlasmaQuick::SharedQmlEngine m_engine;
+    ControllerInput *m_controllerInput;
     static bool s_testMode;
 };
 

From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Antheas Kapenekakis <git@antheas.dev>
Date: Mon, 24 Aug 2026 16:44:38 +0200
Subject: support one-shot autologin overrides

---
 src/daemon/Display.cpp | 72 ++++++++++++++++++++++++++++++++++++++++--
 src/daemon/Display.h   |  1 +
 2 files changed, 70 insertions(+), 3 deletions(-)

diff --git a/src/daemon/Display.cpp b/src/daemon/Display.cpp
index 92feddb249b0..d9dfbe254966 100644
--- a/src/daemon/Display.cpp
+++ b/src/daemon/Display.cpp
@@ -18,6 +18,7 @@
 
 #include "Display.h"
 
+#include "Constants.h"
 #include "DaemonApp.h"
 #include "DisplayManager.h"
 #include "Greeter.h"
@@ -31,6 +32,8 @@
 #include <QLocalSocket>
 #include <QTimer>
 
+#include <optional>
+
 #include <pwd.h>
 #include <sys/time.h>
 #include <unistd.h>
@@ -54,6 +57,63 @@ static int s_ttyFailures = 0;
 
 namespace PLASMALOGIN
 {
+namespace
+{
+struct NextSessionRequest {
+    QString user;
+    Session session;
+};
+
+std::optional<NextSessionRequest> takeNextSessionRequest()
+{
+    const QString requestPath = QStringLiteral(RUNTIME_DIR) + QStringLiteral("/next-session.conf");
+    if (!QFile::exists(requestPath)) {
+        return std::nullopt;
+    }
+
+    const QString claimedPath = requestPath + QStringLiteral(".consumed");
+    QFile::remove(claimedPath);
+    if (!QFile::rename(requestPath, claimedPath)) {
+        qWarning() << "Unable to claim next session request" << requestPath;
+        return std::nullopt;
+    }
+
+    QString user;
+    QString sessionName;
+    {
+        KConfig requestConfig(claimedPath, KConfig::SimpleConfig);
+        const KConfigGroup autologinGroup(&requestConfig, QStringLiteral("Autologin"));
+        user = autologinGroup.readEntry(QStringLiteral("User"));
+        sessionName = autologinGroup.readEntry(QStringLiteral("Session"));
+    }
+
+    if (!QFile::remove(claimedPath)) {
+        qWarning() << "Unable to remove consumed next session request" << claimedPath;
+    }
+
+    if (user.isEmpty() || sessionName.isEmpty()) {
+        qWarning() << "Next session request is missing its user or session";
+        return std::nullopt;
+    }
+
+    if (!getpwnam(qPrintable(user))) {
+        qWarning() << "Unable to find user from next session request" << user;
+        return std::nullopt;
+    }
+
+    Session session = Session::create(Session::WaylandSession, sessionName);
+    if (!session.isValid()) {
+        session = Session::create(Session::X11Session, sessionName);
+    }
+    if (!session.isValid()) {
+        qWarning() << "Unable to find session from next session request" << sessionName;
+        return std::nullopt;
+    }
+
+    return NextSessionRequest{user, session};
+}
+} // namespace
+
 bool isTtyInUse(const QString &desiredTty)
 {
     if (Logind::isAvailable()) {
@@ -127,8 +187,14 @@ Display::Display(Seat *parent)
     });
     connect(m_greeter, &Greeter::displayServerFailed, this, &Display::displayServerFailed);
 
-    // Load autologin configuration (whether to autologin, user, session, session type)
-    if ((PlasmaLogin::config()->autologinRelogin() || daemonApp->tryLockFirstLogin()) && !PlasmaLogin::config()->autologinUser().isEmpty()) {
+    // A next-session request overrides the persistent autologin configuration once.
+    if (const auto request = takeNextSessionRequest()) {
+        m_autologinUser = request->user;
+        m_autologinSession = request->session;
+        daemonApp->tryLockFirstLogin();
+    } else if ((PlasmaLogin::config()->autologinRelogin() || daemonApp->tryLockFirstLogin()) && !PlasmaLogin::config()->autologinUser().isEmpty()) {
+        // Load autologin configuration (whether to autologin, user, session, session type)
+        m_autologinUser = PlasmaLogin::config()->autologinUser();
         // determine session type
         QString autologinSession = PlasmaLogin::config()->autologinSession();
         m_autologinSession = Session::create(Session::WaylandSession, autologinSession);
@@ -174,7 +240,7 @@ bool Display::start()
     // (rootful X + X11 autologin session).
     if (m_autologinSession.isValid()) {
         m_auth->setAutologin(true);
-        if (startAuth(PlasmaLogin::config()->autologinUser(), QString(), m_autologinSession)) {
+        if (startAuth(m_autologinUser, QString(), m_autologinSession)) {
             return true;
         } else {
             return handleAutologinFailure();
diff --git a/src/daemon/Display.h b/src/daemon/Display.h
index bdfa1cd35f0b..ec2af1571de1 100644
--- a/src/daemon/Display.h
+++ b/src/daemon/Display.h
@@ -82,6 +82,7 @@ private:
     QString m_passPhrase;
     QString m_sessionName;
     QString m_reuseSessionId;
+    QString m_autologinUser;
 
     Session m_autologinSession;
 
